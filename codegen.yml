schema: http://localhost:3000/api/graphql
generates:
  ./apollo/server/types/graphqlTypes.generated.ts:
    config:
      strictScalars: true
      scalars:
        Timestamp: "firebase-admin/firestore#Timestamp"
    plugins:
      - typescript
      - typescript-resolvers